# Performance

Keep dependencies minimal, use supplied assets directly, keep visuals CSS-based where practical, avoid heavy animation, use static blog generation and optimize real portfolio/media assets before production. Run Lighthouse/Core Web Vitals against the deployed site.
