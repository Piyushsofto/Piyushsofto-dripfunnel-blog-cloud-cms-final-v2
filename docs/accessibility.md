# Accessibility

Semantic HTML, keyboard navigation, visible focus states, accessible menu state, labeled form fields, required validation, native FAQ details/summary, logo alt text, approximately 44px touch targets, contrast and reduced-motion support are implemented.
